# Development revision v2 — before v2 execution
V1 winter results were inspected; v1 summer execution is in progress. V2 is adaptive development, never independent confirmation. Retain all v1 results and hashes.
RQ-BPI-03: Does a response-time rule tied to the identified process improve overly conservative initial auto PI? V1 used lambda=600 s while fitted tau is about 250 s. V2 sets lambda=max(3*sampling_interval, tau/2), determined only by the 2 h fitted model. This shortens the design response time without a gain grid. No new plant identification data. Fit coefficients, phase splits, targets, constraints, metrics, dates and 24 h warmup unchanged.
V2 uses no fitted static environmental feedforward when the calibration design lacks full rank / solar excitation. This guard is an identifiability decision, not selection by a summer score. In this pilot it leaves plain PI; future full-rank data are necessary to evaluate richer environment learning. No neural improvement claimed.
Run exactly two fresh-FMU episodes (days 3 and 180). Compare all v1 methods, report every result. Exploratory practical comparison: v2 MAE <= selected grid MAE+0.05 C AND v2 max error <= grid max error+0.5 C on both dates, with v2 time within +-0.5 C also displayed. This margin is newly specified after viewing v1 and is not a preregistered thesis success threshold. No confidence interval or human-time-saving claim. If margin fails, retain the failure and do not search further during this pilot.
Producer scripts/run_boptest_rapid_pi_v2.py; outputs/boptest_rapid_pi/v2 traces; artifacts/result_v2.json. Simulation exposure 2 h for calibration; v2 developer iteration/evaluation overhead separately disclosed and not hidden as total research cost.
