# Reproduction
From school: clone https://github.com/ibpsa/project1-boptest.git --branch v0.9.0 --depth 1 into outputs/vendor/boptest-v0.9.0. Preserve upstream license.md and library_versions.json; no FMU redistribution in committed evidence.
Create outputs/boptest-venv; install fmpy==0.3.22. Freeze installed dependencies with evidence. Run outputs/boptest-venv/bin/python scripts/run_boptest_rapid_pi.py. Simulator is Linux x86_64 bundled co-simulation; do not invent REST KPIs. No random draws in v1. Hash protocol, runner, FMU and traces. Run python3 -m unittest discover -s tests and python3 scripts/validate_research_openspec.py. Report missing dependencies or external service failures explicitly.

## Local compatibility dependency
The bundled FMU links libgfortran.so.4 (also listed in upstream service/worker/Dockerfile). Host provides .5 only. Downloaded from https://archive.ubuntu.com/ubuntu/pool/universe/g/gcc-7/libgfortran4_7.5.0-6ubuntu2_amd64.deb and extracted with `dpkg-deb -x` into outputs/vendor/boptest-runtime, without system installation. Run with `LD_LIBRARY_PATH="$PWD/outputs/vendor/boptest-runtime/usr/lib/x86_64-linux-gnu" outputs/boptest-venv/bin/python scripts/run_boptest_rapid_pi.py`. Shell PWD assumes the school project directory. Retain extracted copyright notices.
The runner refuses overwriting result.json. For a clean rerun, use a clean checkout/output directory; preserve an existing result plus traces under a new attempt directory before rerunning. No seed-dependent outcomes exist in v1.
Attempts 01 (missing shared library) and 02 (infinite condition diagnostic not JSON-serializable) were retained. Attempt 02 calibration CSV is retained; the diagnostic fix records null condition number and matrix rank when singular, without altering coefficient fitting or protocol.

## Final pilot commands
After v1, run the same local LD_LIBRARY_PATH with `outputs/boptest-venv/bin/python scripts/run_boptest_rapid_pi_v2.py`. Run `python3 scripts/verify_boptest_rapid_pi.py` to audit both revisions. Generate the final combined report with `python3 scripts/build_boptest_briefing.py` (system matplotlib 3.6.3); do this after v1's preliminary report generator. All report links resolve from docs/reports. V2 protocol was written after v1 winter results and before v2 execution. Retained machine results must never be relabelled independent confirmation.
