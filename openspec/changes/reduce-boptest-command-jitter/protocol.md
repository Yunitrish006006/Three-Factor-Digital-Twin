# BPJ-P1 — before simulation
Official pinned bestest_air, same 2h fit,60s step,22C target,fan0.5,supply12–40C,24h warmup and24h full scoring. New adapter supports command transport delay0 or1 steps, no continuous actuator lag. Delay queue initially contains heat-equivalent handover supply. Log requested command and actual applied command separately. Noise is Gaussian SD0.05C with common Random(52000+day), scored against FMU true T. Controller may see measured T and actual previously applied supply; no future information.
Compare v4 unchanged; aware1: same observer using previous actually applied supply instead of previous requested output; smooth06/smooth035: aware1 plus requested supply exponential smoothing alpha0.6/0.35. First action equals handover; back-calculation integral correction0.1*(smoothed-raw). No fitted gains change. Record implementation/source hashes before transfer.
Development: day95 and245, noise0.05, delays0/1 (4 cases x4 controllers=16). Select among aware1/smooth06/smooth035 by minimum macro requested-command total variation, provided EVERY case MAE<=v4+0.02C and max error<=v4+0.2C, and macro variation<=80% of v4. Tie order aware1,smooth06,smooth035. Fallback v4 if none qualify. Save selection before transfer.
Transfer: unused day65/155/275 each (noise0,delay0) and (noise0.05,delay1), 6 cases. Compare v4, selected, grid handover (same prior6/300 gains), deduplicate if fallback. No retuning after opening. No resampling seeds to seek wins.
Primary gate: each transfer case MAE<=v4+0.02C and max<=v4+0.2C AND mean requested variation in noisy/delayed cases<=80% of v4. Report each case, clean/noisy groups, requested and applied variation, saturation, energy channels, initial/post30min MAE and temperature excursions. Keep adverse and failed results. New dates same plant are not independent hardware evidence. Grid receives same delay/noise information and handover, but no observer. Pure smoothing versus delay awareness separated in development.
Producer scripts/run_boptest_jitter.py phases development/transfer; artifacts in this change; outputs/boptest_jitter CSV. No original source edits. If any phase fails preserve result and do not overwrite.
