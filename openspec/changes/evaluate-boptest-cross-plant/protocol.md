# BCP-P1 — specified before either generalized runner plant is simulated
Pin BOPTESTv0.9.0 FMUs bestest_air and bestest_hydronic. Air is development reference; hydronic has not been simulated in this project. Inspect official interfaces before choosing settings. Freeze entire shared algorithm and both device configs before any runs; no edits after air or hydronic scores. Device configuration supplies FMU path, sensor/actuator names, Celsius supply limits(air12–40,hydronic20–80), auxiliary setting(air fan0.5,hydronic pump1), target22C, step60s and measured-temperature meaning. Auxiliary fixed throughout24h warmup AND scoring, so normalized outgoing supply provides a shared handover without an air-only heat formula. Hydronic is heating-only, max heater5kW; winter evaluation days10/40 chosen in advance for both. No claim about summer cooling or humidity.
Actions normalized u in[0,1] mapped by config to supply temperature; controller never reads plant name. Shared fit: deltaT=l*(-(T-22))+b*(u-.5)+c*(Tout-22)+d. Ridge1e-8, same columns, chronological2h prefix/full6h of one calibration. Reject if not0<l<1 or b<=0 or nonfinite; record rejection and NOT_EVALUATED adaptive cases, no ad hoc repair. No transfer of old numerical Kp/Ti/b. Physical first-order approximation may fail for slow hydronics.
Calibration starts day1 after24h common warmup. Normalized excitation is initial outgoing u plus repeating[-.15,.15,0,.25,-.25,.10],15min blocks, clipped0..1, total6h. Costs:2h/6h usable prefixes, but6h actually collected. No human data. Retain all temperature excursions; not a safe real-plant excitation prescription.
For each valid budget, tau=-dt/log(1-l), K=b/l. Shared auto-PI: Ti=tau,Kp=tau/(K*lambda),lambda=max(3*dt,q*tau), q in[.5,1]. Same conditional antiwindup and matched handover. Proposed method adds same causal disturbance observer(update0.2), request smoothing0.35 and backcalculation0.1; observer removes b*(u_prev-.5) from deltaT, carries disturbance bias .5-dhat/b. Initialize dhat=-b*(u0-.5), align integral exactly at initial measurement; no plant-specific branches.
Two allowed trials per method/budget: each2h at day2 after fresh24h warmup. Select smallest trial MAE; tie orderq=.5 before1. Auto-PI and proposed each get identical data/trial count. Calibration exposure2/6h +4h trial exposure =6/10h total active adaptation budget per method. One scalar q adjusted; l,b,c,d fitted automatically. Observer/filter constants fixed globally from prior development; disclose provenance, do not call universal defaults. Wall clock and common warmup separately reported.
Freeze selected q/fit and hashes before evaluation on days10/40 (24h each). Same model-specific true measurement, actuation limits, auxiliary values, initial state and no added sensor noise. Shared idealized supply actuation remains. Scores: MAE/RMSE/max error,within±0.5C,normalized requested-TV,saturation,domain excursions,energy channels separately,first30min/post30minMAE. Per-plant two-date conclusions only, no IID-minute inference. Gain=auto-PI MAE minus proposed MAE; exploratory nonregression requires each date proposedMAE<=baseline+.02C and max<=baseline+.2C with mean positive gain. No claim based solely on2h beating6h (different information budget).
Runner scripts/run_boptest_cross_plant.py --plant air or hydronic; config scripts/boptest_cross_plant_devices.json; core digital_twin/control/portable_pi.py. Root freeze artifact must exist before run; refuse overwritten plant result. Separate calibration/tuning and selected JSON written before eval. All failures and unfinished cases persist. Two plants, at most34 episodes: each1calibration+8trials+8eval; invalid fits reduce count visibly. No coefficient repair, additional candidates or result-driven q after scores. Test suite, trace/source/unit/selection verification and offline HTML required.
