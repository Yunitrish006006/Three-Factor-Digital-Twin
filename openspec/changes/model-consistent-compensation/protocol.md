# MCC-001 Protocol — freeze before any new score or simulation
Reuse frozen bank selections and calibration prefixes from improve-portable-dynamics (air,hydronic) and evaluate-more-boptest-devices (heat_pump,apartment,commercial). All five are development plants. Apartment6h rejected model is not repaired, and its controls remain skipped. No new identification or bank search, no new trial selection. New within-plant dates25/60,24h each, same target22C,dt60s,24h warmup and exact existing installation settings converted declaratively to native units. Models and settings frozen before all runs. Both methods use q=.5, so active calibration exposure is2h/6h and trial exposure0h; do not compare this to the old selected-q baselines as paired evidence or claim saved time-to-quality.

Methods: auto_pi (same original PI equations, hybrid off) versus consistent (full-model compensation). At observation k, predict T_k from measured T_(k-1-j), actual u_(k-1-delay), outdoor_(k-1), and every fitted ARX coefficient. No residual until required measured histories exist. Filter innovation with beta=1-exp(-dt/max(600,tau_s)); filter squared innovation with same beta. All observer state updates are causal. Predictive dynamics are not physical-parameter truth.

Compute H=10-step unit disturbance response L_H and input step response G_H using the selected ARX recurrence, zero initial perturbations, and its input delay. Compute innovation noise amplification as sqrt(sum of squares of ten impulse coefficients). sigma_H=max(.02C,stored10-step calibration validation RMSE,sqrt(max(EMA(r²)-EMA(r)²,0))*noise_amplification). These are heuristic uncertainty scales, not confidence intervals. Correction authority cmax=.1 normalized command; confidence=(cmax*G_H)^2/((cmax*G_H)^2+sigma_H²). If G_H<=0 or confidence<.2 use zero desired correction; otherwise desired=-G_H*(L_H*EMA(r))/(G_H²+(sigma_H/cmax)²), clipped±.1. Limit correction change to±.01 per60s. No separate whole-command smoothing. PI integral uses conditional integration against the combined tentative command, with no observer on auto_pi. Prime preserves actual actuator handover; disabled correction matches auto-PI equations.

All constants shared across plants; no device-name branch in controller. Ten-minute horizon equal to validation horizon intentionally cannot compensate devices with identified delay>=10 steps; disclose this scope rather than hiding fallback as improvement. No post-result threshold, model, bound or q change.

Up to36 evaluations:9 admissible budgets x2methods x2dates. Four apartment6h episodes skipped. Each completed episode48 simulated h including warmup; maximum1728h aggregate. No new trial/calibration simulations. Freeze new controller/runner/adapter/config/protocol, old controller sources, old results/calibration CSVs/FMUs. Preserve per-step prediction,innovation,correction,confidence,sigma for audit; no future weather.

Metrics: within-device paired MAE,RMSE,maxabs,within.5C,requestedTV,saturation,outside20..30C,separate power integrals, warmup and runtime. Gate per valid budget requires both dates MAE<=baseline+.02C,maxabs<=baseline+.2C,TV<=1.25*baselineTV+.1, and positive mean MAE gain. Exact fallback equality is nonregression, NOT improvement. Overall H-MCC-01 needs all nine valid budgets gated; invalid apartment6h remains an applicability failure. No pooled operative/air MAE or statistical-significance claims. Preserve all adverse,skipped,failed evidence; no retries after model/control failure.

Artifacts: scripts/run_boptest_consistent.py, scripts/boptest_consistent_adapter.py, scripts/boptest_consistent_devices.json, digital_twin/control/consistent_pi.py; artifacts/{plant}.json,freeze.json,verification.json; outputs/boptest_consistent/{case}/*.csv; docs/reports/boptest_consistent_2026-09-08_zh.html. ResearchWorkspace owns typed 3D graph links, not duplicated thesis artifacts.
