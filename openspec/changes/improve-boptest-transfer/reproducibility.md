# Reproduction
From school: `env LD_LIBRARY_PATH="$PWD/outputs/vendor/boptest-runtime/usr/lib/x86_64-linux-gnu" outputs/boptest-venv/bin/python scripts/run_boptest_transfer.py --phase development`, followed by same command with `--phase transfer`. Same FMU, FMPy0.3.22, local libgfortran4 and upstream licenses as pilot-boptest-rapid-pi. No new download. Results preserve source/input/protocol hashes. `python3 -m unittest discover -s tests`, OpenSpec validator and new trace verifier required. HTML report generated from JSON, no PPTX. Large CSV remain in ignored outputs; machine summaries retained.

After both phases: `python3 scripts/verify_boptest_transfer.py`, then `python3 scripts/build_boptest_transfer_report.py` (system matplotlib3.6.3). `development.json` retains all eight candidate episodes; selection.json references its complete hash and exact runner/protocol/prior/FMUs. Transfer aborts if any differ. Final verifier independently recomputes all CSV metrics and noise sequence using Python random.Random(42000+day). Controller v2 development metrics must exactly match prior result_v2.json, checking adapter parity. Warmup input defaults were made valid before override activation; no resulting legacy metric change is accepted.
