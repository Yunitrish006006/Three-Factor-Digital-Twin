# MDV-001 Frozen additional-device protocol
Before any new-device simulation, freeze new runner, generic adapter, device JSON, this protocol, both existing controller modules, prior delayed-round freeze/results, and all three official wrapped FMUs plus local documentation hashes. No controller edits, manual gain repairs, or changes after calibration/evaluation. Devices selected from local official BOPTEST v0.9.0 metadata: bestest_hydronic_heat_pump (compressor 0..1, fan/pump 1, operative temperature); twozone_apartment_hydronic (supply 273.15..318.15 K, day valve 1, both thermostat targets 295.15 K, other controls native; day-zone air only); singlezone_commercial_hydronic (radiator valve 0..1, pump 25000 Pa, supply 318.15 K, zone target295.15 K, AHU native). All overrides apply during warmup except the primary actuator. These are predeclared installation settings, not learned controller constants.

Same target22C, step60s, 24h native-primary warmup before each episode. Primary handover uses readback normalized with configured native bounds. Day1 six-hour calibration, repeat initial_u+[-.15,.15,0,.25,-.25,.10] every15min, clip0..1. Use prefixes2h/6h. Frozen identify_bank and tuning/DelayedPortablePI from prior round: orders1/2 x delays0/1/3/5/10; first70% eligible training endpoints, disjoint10-step free rollouts, pole/gain gate and full-prefix refit. Same q .5/1, same two 2h day2 trials per method, same fixed observer/smoothing/backcalculation. Save pre-evaluation selection and adaptation snapshot. Then evaluate days20/50,24h each. No look-ahead weather or retuning. Device-specific names never reach controller.

Both methods share calibration and have equal tuning budget. Per-method active exposure6h/10h excludes24h per-episode warmup; report all calibration/trial/evaluation hours and offline fit/runtime costs. Each new FMU has at most17 episodes (1calibration+8trials+8evaluations); all3 up to51. Retain invalid model banks; skip their controls without replacement. Runtime/interface failures remain failures, not proof of controller inferiority; any needed later implementation repair is a separate disclosed study. Record all completed traces and partial trace on Python exceptions.

Primary units: each device x ID budget x evaluation day. Report MAE,RMSE,maxabs,within0.5C,TV,saturation,outside20..30C,and separate power integrals. Gate per device/budget: both days MAE<=autoPI+.02C and max<=autoPI+.2C, plus positive mean MAE gain. Missing pair fails completeness, never zero error. Whole three-device generality criterion requires all six budgets gated; no pooled air/operative score. Days are deterministic scenario samples, no significance claim. All still building HVAC, apartment is one controlled zone with native coupled night zone, not full multizone optimization. Official FMUs via custom FMPy runner; not REST/KPI equivalent. No manual-labor/time-to-quality saving established by this comparison.

Evidence paths: scripts/run_boptest_more_devices.py; scripts/boptest_native_adapter.py; scripts/boptest_more_devices.json; artifacts/{heat_pump,apartment,commercial}.json and verification.json; outputs/boptest_more_devices/*/*.csv; offline docs/reports/boptest_more_devices_2026-09-08_zh.html. Do not overwrite prior rounds.
