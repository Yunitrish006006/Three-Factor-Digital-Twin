# Reproduction
Use Python hashlib.sha256 on every path in selection.json sources; compare development.json hash and transfer.json selection hash. Read controller class bodies and make_controller factories, not only scenario loops. Inputs are local existing evidence; no network needed. Run OpenSpec validator after audit documentation.
