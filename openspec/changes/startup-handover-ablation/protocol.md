# SHA-001 frozen protocol
Five known BOPTESTv0.9.0 devices, original accepted2h models, q=.5, native settings and21→22C24h warmup from SPB.60s timestep. Same input/config for baseline and four factorial policies. New sources, inherited controller/tuning/metrics, priorbaseline JSON/CSV, models/calibration, FMUs and this protocol SHA256 frozen before simulation. No device names in controller.
OrdinaryPI core uses original conditional integration on its own unsaturated command. Initial integral gives actual actuator handover. Assistance starts only initialabs(error)>.1C, desired correction=clip(Kp*error,±.1u), slew.02u/min. No predictive exit or integral-catchup attenuation: these are removed for both modes equally. On first crossing of initial error sign or declared duration15/55min (whichever first), irreversible handover begins. At index0 keep initialu. Durations are intended maximum active periods, not guaranteed different when target crosses sooner.
Modes: taper retains ordinaryPI integral and slews correction tozero at.02u/min; retain transfers current correction intoPI integral then sets external correctionzero before ordinary integration at that observation. Track pre/post-transfer unsaturated command mismatch; no instantaneous jump due transfer, but normal observation/integration still changes output. Duringactive and taper output=clamp(.5+Kp*error+I+correction). Under retain afterhandover output=ordinaryPI. Both modes thereafter ordinaryPI once correctionzero, not necessarily same state/trajectory as standalone baseline. Taper correctionzero≤duration+5min≤60min. Record trigger,actualzero,mode,duration,integral,correction,output and handover mismatch.
Development day8,3h for each of4 policies:15_taper,15_retain,55_taper,55_retain. Reuse verified SPB original same-configuration baseline explicitly; do not rerun/count it.20 newdevelopment episodes,540aggregate simh. New12h candidate trial/device plus existing3h baseline observation+2h identification=17h nominal exposure; not a lower tuningcost claim, priorresearchcosts remain extra.
Use unchanged SPB first60min/latefixed windows and composite gates: earlyMAE improvement≥max(.001C,1%); early/late peak≤baseline+max(.02C,5%); lateMAE≤baseline+max(.002C,5%); lateTV≤1.1baseline+.02; baseline15min acquisition within±.1C must not worsen>1min. Missingacquisition null/rightcensored. Select minimumearlyMAE eligible; ties prefer shorter duration then taper. None eligible selects purePI. Compute all four fixed-factor development contrasts and source pointers; actualsameprefix beforeearliertrigger verifies modesonlydifferathandover. Differences within same deterministicFMU are scoped intervention contrasts, not uncertainty estimates acrossrealdevices.
Freeze all five selections and development hashes before opening days48/83,24h each. Always execute baseline; selectedcandidate only if forwarded, otherwise explicitsame-datebaseline reuse. No confirmationretuning.10..20uniqueconfirmationepisodes (480..960simh),30..40newtotal (1020..1500simh). Samecriteria on eachdate; fallbackpreservation neverimprovement. Retain energy,saturation,peak,domainviolations and absolute errors. No averagingair/operativetemperatures.
Artifacts: digital_twin/control/handover_pi.py; scripts/run_boptest_handover.py,select_boptest_handover.py,boptest_handover_adapter.py; artifacts/{plant}_{phase}.json,freeze.json,selection.json,verification.json; outputs/boptest_handover; docs/reports/boptest_handover_2026-09-08_zh.html. CustomFMPyFMU runner, not REST/KPIequivalence or realhardware/RH/EUV/NN evidence. Fourpoliciescomparefactorcombinations; only selectedpolicyconfirmed, so developmentfactorconclusiondoesnotautomaticallygeneralizetonewdates.
