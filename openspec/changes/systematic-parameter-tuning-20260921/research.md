# Research questions

1. 候選 API 是否提供可重設、可記錄且可重複的 action→response 介面？
2. 固定調教預算下，簡單 PI/PID 是否已足以作為公平 baseline？
3. 在不把 validation 回饋給搜尋的前提下，調教結果是否仍可在 holdout 維持？

目前三題均為 `NOT_EVALUATED`；本文件只登記問題與方法，不預先承諾改善。
