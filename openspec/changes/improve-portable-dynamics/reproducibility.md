# Reproduction
Use existing FMPy0.3.22 and local libgfortran4 LD_LIBRARY_PATH, outputs/boptest-venv/bin/python scripts/run_boptest_delayed_dynamics.py --plant air then hydronic. Freeze source and prior calibration hashes first. Retain all results, run unittest/OpenSpec/verifier, generate HTML with system matplotlib3.6.3. No downloads.
