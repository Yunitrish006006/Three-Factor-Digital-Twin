# PDM-P1 before bank scores
Reuse exact6h calibration CSVs and2h/6h prefixes from evaluate-boptest-cross-plant. Zero new excitation. Freeze all sources before identifying either plant. Hydronic is now development, not untouched confirmation.
Candidates order[1,2] x input-delay[0,1,3,5,10] steps, total10. Model T_next-target=sum(a_j*(T_(k-j)-target))+b*(u_(k-delay)-.5)+c*(Tout_k-target)+d. Ridge1e-8. Shared endpoint start k=11. First70% eligible endpoints train; remaining endpoints scored as disjoint10step free rollouts; drop common incomplete tail. Planned excitation u is known, outdoor held at block-start value, measured T history only at block start; no teacher forcing. Rank rolloutRMSE, ties smallerorder then smallerdelay. Training and full-prefix refit must both have0<max_abs_pole<.999999,b>0,positive finite steady gain; no pole/coef repair. First ranked admissible refit selected; reject if none. Record all candidates/rejections, fit counts and validation endpoints. Refit uses same prefix only.
Tau=-dt/log(max_abs_pole),K=b/(1-sum(a)). Shared tuning lambda=max(3dt,q*tau,delay*dt),Kp=tau/(K*(lambda+delay*dt)),Ti=tau,q in[.5,1]. One bounded control adjustment q; order/delay/coefficients identified automatically. Observer update.2,smoothing.35,backcalculation.1 remain fixed. Generic delay history passes delayed actual prior u to the same PortablePI observer; initialize unknown pre-control history at handover. Auto-PI and proposed(observer+smoothing) receive same fitted model and budgets. Controller never receives plant name.
Per validplant/budget, eachmethod gets two2h day2 trials with24h warmup, selects q byMAE, ties.5. Save model/selection/source hashes before24h evaluation at unuseddays20/50. No additional candidates or post-evaluation tuning. Same prior device JSON,normalized actions,auxiliary settings,target22,step60; no new noise or artificial delay.
Cost usable2h/6h identification plus4h trial exposure per method;10candidate fits plus up to10refits extra offline work. Up to32new FMU episodes,zero new calibration. Report perdevice air versus operativeT separately, MAE/RMSE/max,TV,saturation,energychannels,domain excursions and skips. Gate perdateMAE<=autoPI+.02C,max<=autoPI+.2C pluspositive meanMAE gain perplant/budget. It is exploratory, not statistical equivalence. Good prediction alone cannot establish control performance.
Core digital_twin/control/delayed_dynamics.py; runner scripts/run_boptest_delayed_dynamics.py --plant air/hydronic. Raw outputs/boptest_delayed_dynamics; currentchange artifacts. Full unittest,OpenSpec,candidate/trace/hash audits and offlineHTML. Another untouched plant required for stronger generality.
