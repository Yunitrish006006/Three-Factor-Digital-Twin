# PSB-001 preregistration
Five known FMUs and prior2h models/configs from SPB-001. Same21→22C warmup challenge,60s sampling,q=.5. Freeze controller/runner/selector/adapter, inherited dependencies, previous baseline results/traces, models/calibration, protocol and FMUs before simulation. No plant identity in controller.
Baseline ordinary PI. New helper uses an ordinary PI core with its original conditional integration evaluated at its own command (not combined command). No injection of correction into its integral. Track initial integral I0, error sign s, and causal EMA temperature slope beta=1/3. Initial error within±.1C skips assistance.
Active desired correction is s*max(0,min(.1,Kp*abs(error))-max(0,s*(I-I0))). This reduces assistance as ordinary PI integral catches up. Irreversible withdrawal begins if s*error-max(0,s*slope)*horizon_seconds<=.1C, error crosses initial sign, desired correction becomes zero after at least5min, or55min elapsed. Candidate horizons3,10,30min; all other constants shared. Correction slews toward desired (or zero during withdrawal) by at most.02u/min; at most5min withdrawal guarantees zero by60min. Record trigger and actual zero-correction times separately. No instantaneous state transfer; after zero correction the PI core alone controls. Its state follows the assisted temperatures, so it need not equal the standalone baseline.
Development day8,3h per candidate, three candidates/device. Reuse original SPB same-configuration day8 baseline with explicit source reference and SHA256; no duplicate baseline run. New15 episodes,405 aggregate simh; new trial exposure9h/device plus existing3h baseline observation and2h calibration (14h cumulative nominal adaptation,11h if only new trials+existing ID counted; distinguish). Prior SPB study search costs are sunk but must not be hidden when describing cumulative research cost.
Use unchanged SPB windows and gate: first60min MAE gain≥max(.001C,1%); early/late maxima≤baseline+max(.02C,5%); lateMAE≤baseline+max(.002C,5%); lateTV≤1.1base+.02; baseline15min acquisition within±.1C cannot worsen>1min. Select min earlyMAE among eligible, tie prefers smaller horizon; none chooses PI. Also report whether proposed candidates outperform previous SPB candidates on development day; not independent confirmation.
Freeze all five selections and development SHA256 before confirmation. New days42/77,24h each. Always run baseline; selected candidate only if not fallback, otherwise explicit reference reuse. No retuning after new dates opened.10..20 unique confirmation episodes,480..960 aggregate simh. Total25..35 newepisodes,885..1365simh. Exact-equal candidate trajectories do not identify a horizon; passing relative gates does not establish absolute precision. Acquisition null remains right-censored. Retain failures, energy, saturation, peak and domain violations. Do not pool air/operative temperature absolute MAEs.
Artifacts: digital_twin/control/predictive_startup_pi.py; scripts/run_boptest_predictive_startup.py,select_boptest_predictive_startup.py,boptest_predictive_startup_adapter.py; artifacts/{plant}_{phase}.json,freeze.json,selection.json,verification.json; outputs/boptest_predictive_startup; docs/reports/boptest_predictive_startup_2026-09-08_zh.html. Same officialBOPTESTv0.9.0/FMPy custom runner; no REST/KPI,realhardware,RH,EUV,NN claim.
