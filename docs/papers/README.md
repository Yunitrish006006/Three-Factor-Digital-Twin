# Papers 與參考來源

- `ieee/`：英文 IEEE 稿來源、參考文獻與正式 PDF。
- `thesis/`：中文論文正式成品與建置資產。
- `reference_sources/`：外部論文、文字轉錄與課程報告輔助來源。

`reference_sources/` 只保存外部材料，不應被描述為本研究的實驗輸出。LaTeX 的 `aux`、`log`、`bbl`、`blg` 等中繼檔不納入版本控制；正式 PDF 由既定建置命令重新產生。
